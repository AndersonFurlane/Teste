<h1>Listing posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Excerpt</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <!--quando tivermos o erb:
  <% %> seguido de qualquer instrulçao ruby ele ira executar o trecho e nao vai
  mostrar nada quando temos, nao gera HTML
  <%= %> ele executa o codigo e mostra o resultado dele  -->
<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= truncate post.excerpt %></td> <!-- truncate trunca a string-->
    <td><%= link_to 'Show', post %></td> <!--ou dentro do <td></td> colocar
    <a href="/post/<%#= post.id%>" para fazer link-->
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
